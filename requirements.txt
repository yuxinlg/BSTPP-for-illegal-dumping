# Runtime stack: single source in requirements-runtime.txt (also setup.py).
-r requirements-runtime.txt

# Extra host packages commonly present alongside the pinned runtime stack.
etils
flatbuffers
kiwisolver==1.4.4
Pillow>=9.2.0
seaborn>=0.11

# Notebook (cbg-park-seasonal) plotting dependencies:
contextily>=1.3
matplotlib-scalebar>=0.8
# rasterio pinned <1.4: rasterio>=1.5 requires numpy>=2, which conflicts with
# the numpy<2 pin above (forced by jax 0.4.23). Pulled in by contextily.
rasterio<1.4

# Dev tooling: linter used for the refactor. Pure binary wheel, no Python deps,
# so it does not interact with the numpy<2 / jax 0.4.23 pins above.
ruff==0.15.21

# Dev tooling: YAML parser for the Phase 3f completion-manifest validator
# (docs/phase3f_completion_manifest.yaml), which is a standing gate on every
# commit that changes a Phase 3f status, scope, dependency, blocker, cycle count
# or exit gate. NOT a package dependency -- setup.py reads
# requirements-runtime.txt only, and this must not be added there. `pip show`
# reports an empty Requires, so it pulls in nothing and cannot interact with the
# numpy<2 / jax 0.4.23 pins above.
PyYAML==6.0.3
