A-45 RED CAPTURE -- CI-9 enforcement, WP2's first commit.

FIRST EXERCISE OF D-45 (capture-then-fix), and it should be visible as such:
this file is written from the PRE-CHANGE tree, before bstpp/ is touched,
and is committed as-is. Nothing is re-run over it after the fix.

The pre-change state is the REAL ancestor b98e91d -- no revert had to be
constructed, because the rows enforce something the tree has never done.

Machine-local, this repo, run 2026-08-06T17:58:19Z.
git rev-parse --short HEAD : b98e91d
git status --porcelain (tracked, before the fix):
  (none)

================================================================
RED -- tests/test_ci9_enumerated_value.py at b98e91d
================================================================
FFF...FFFF.                                                              [100%]
================================== FAILURES ===================================
_________________ test_rejected_without_covariates[nonsense] __________________

value = 'nonsense'

    @pytest.mark.parametrize("value", BAD)
    def test_rejected_without_covariates(value):
        """THE FIRING ROW. No covariates, so the consuming leg never runs -- and
    
        the value is still not one the package accepts.
        """
>       with pytest.raises(ValueError, match="standardize_cov"):
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       Failed: DID NOT RAISE ValueError

tests\test_ci9_enumerated_value.py:80: Failed
___________________ test_rejected_without_covariates[True] ____________________

value = True

    @pytest.mark.parametrize("value", BAD)
    def test_rejected_without_covariates(value):
        """THE FIRING ROW. No covariates, so the consuming leg never runs -- and
    
        the value is still not one the package accepts.
        """
>       with pytest.raises(ValueError, match="standardize_cov"):
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       Failed: DID NOT RAISE ValueError

tests\test_ci9_enumerated_value.py:80: Failed
____________________ test_rejected_without_covariates[42] _____________________

value = 42

    @pytest.mark.parametrize("value", BAD)
    def test_rejected_without_covariates(value):
        """THE FIRING ROW. No covariates, so the consuming leg never runs -- and
    
        the value is still not one the package accepts.
        """
>       with pytest.raises(ValueError, match="standardize_cov"):
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       Failed: DID NOT RAISE ValueError

tests\test_ci9_enumerated_value.py:80: Failed
_________________ test_legacy_boolean_is_still_named_as_such __________________

    def test_legacy_boolean_is_still_named_as_such():
        """OP-3/OP-4 settled that legacy booleans are rejected EXPLICITLY, never
    
        silently reinterpreted. Consolidating the clause must not lose the word
        that tells a migrating user what happened to their argument.
        """
        for covariates in (False, True):
>           with pytest.raises(ValueError, match="boolean"):
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE ValueError

tests\test_ci9_enumerated_value.py:101: Failed
_____ test_no_standardization_record_survives_a_rejected_value[nonsense] ______

value = 'nonsense'

    @pytest.mark.parametrize("value", BAD)
    def test_no_standardization_record_survives_a_rejected_value(value):
        """The reported record is the reason this is not merely untidy.
    
        Before CI-9 a rejected value yielded a model whose ``.standardization``
        read ``{'method': 'none', ...}`` -- well-formed, and indistinguishable from
        a legitimate ``standardize_cov=None``. Nothing downstream could tell.
        """
        try:
            model = _build(value, covariates=False)
        except ValueError:
            return                                   # rejected: nothing to report
>       pytest.fail(
            f"standardize_cov={value!r} constructed, and the model now reports "
            f"standardization={model.standardization!r} -- a well-formed record "
            "for a value the package does not accept")
E       Failed: standardize_cov='nonsense' constructed, and the model now reports standardization={'method': 'none', 'columns': [], 'mean': None, 'scale': None} -- a well-formed record for a value the package does not accept

tests\test_ci9_enumerated_value.py:117: Failed
_______ test_no_standardization_record_survives_a_rejected_value[True] ________

value = True

    @pytest.mark.parametrize("value", BAD)
    def test_no_standardization_record_survives_a_rejected_value(value):
        """The reported record is the reason this is not merely untidy.
    
        Before CI-9 a rejected value yielded a model whose ``.standardization``
        read ``{'method': 'none', ...}`` -- well-formed, and indistinguishable from
        a legitimate ``standardize_cov=None``. Nothing downstream could tell.
        """
        try:
            model = _build(value, covariates=False)
        except ValueError:
            return                                   # rejected: nothing to report
>       pytest.fail(
            f"standardize_cov={value!r} constructed, and the model now reports "
            f"standardization={model.standardization!r} -- a well-formed record "
            "for a value the package does not accept")
E       Failed: standardize_cov=True constructed, and the model now reports standardization={'method': 'none', 'columns': [], 'mean': None, 'scale': None} -- a well-formed record for a value the package does not accept

tests\test_ci9_enumerated_value.py:117: Failed
________ test_no_standardization_record_survives_a_rejected_value[42] _________

value = 42

    @pytest.mark.parametrize("value", BAD)
    def test_no_standardization_record_survives_a_rejected_value(value):
        """The reported record is the reason this is not merely untidy.
    
        Before CI-9 a rejected value yielded a model whose ``.standardization``
        read ``{'method': 'none', ...}`` -- well-formed, and indistinguishable from
        a legitimate ``standardize_cov=None``. Nothing downstream could tell.
        """
        try:
            model = _build(value, covariates=False)
        except ValueError:
            return                                   # rejected: nothing to report
>       pytest.fail(
            f"standardize_cov={value!r} constructed, and the model now reports "
            f"standardization={model.standardization!r} -- a well-formed record "
            "for a value the package does not accept")
E       Failed: standardize_cov=42 constructed, and the model now reports standardization={'method': 'none', 'columns': [], 'mean': None, 'scale': None} -- a well-formed record for a value the package does not accept

tests\test_ci9_enumerated_value.py:117: Failed
=========================== short test summary info ===========================
FAILED tests/test_ci9_enumerated_value.py::test_rejected_without_covariates[nonsense]
FAILED tests/test_ci9_enumerated_value.py::test_rejected_without_covariates[True]
FAILED tests/test_ci9_enumerated_value.py::test_rejected_without_covariates[42]
FAILED tests/test_ci9_enumerated_value.py::test_legacy_boolean_is_still_named_as_such
FAILED tests/test_ci9_enumerated_value.py::test_no_standardization_record_survives_a_rejected_value[nonsense]
FAILED tests/test_ci9_enumerated_value.py::test_no_standardization_record_survives_a_rejected_value[True]
FAILED tests/test_ci9_enumerated_value.py::test_no_standardization_record_survives_a_rejected_value[42]
7 failed, 4 passed in 21.63s
PYTEST_EXIT:1

================================================================
SECOND RED, unplanned and PRESERVED under D-45 rather than fixed silently
================================================================
Adding a *_invariant_clause to config.py grows the ASCII sweep's clause
population by introspection. The new clause takes a parameter the sweep
has no sample for, so the sweep goes RED -- which is the sweep working as
designed (A-29's M3: an unevaluated clause is an UNCOVERED clause, and
must FAIL rather than skip). Captured before the sample is added.

scanned 17 modules under bstpp/

COVERAGE (D-39: a gate states its own coverage)
  direct raise sites                    : 182
    literal text scanned                : 171  (94%)
    no literal, text from a clause fn   : 10  (covered below by evaluation)
    no literal, ascii_safe()-wrapped    : 1  (covered BY CONSTRUCTION -- listed below)
    no literal, text built at runtime   : 0  (NOT covered -- listed below)
  delegated raise_*() call sites        : 23  (remediation literals scanned)
  *_invariant_clause functions          : 9 evaluated, 1 unevaluated
  => raise sites whose text is checked  : 182/182  (100%)

POPULATION OF THIS RATIO (stated, because 100% invites the wrong
reading of what it covers)
  INCLUDED : ast.Raise nodes in bstpp/*.py -- every site that
             raises, whether its text is a literal, a D-40 clause
             function, or an ascii_safe()-wrapped runtime string.
  EXCLUDED : text that reaches a console WITHOUT being raised.
             KNOWN LIVE INSTANCE -- OP-25: data_contracts.enforce's
             warnings.warn renders the SAME DataContractReport
             .summary() as the raise beside it, to the same console,
             and is NOT ascii_safe()-wrapped. Under data_contracts=
             'report' the identical UnicodeEncodeError is reachable.
             A-37 escaped the raise and deliberately stopped there;
             D-40's corollary names raised messages, and widening it
             is a decision, not a detail.
  EXCLUDED : warnings, logging, print, and __str__/__repr__ on
             package objects. None is swept; none is claimed.
  So: 184/184 means EVERY RAISE SITE, not every console-bound
  string in the package. The second figure is not measured here.

ASCII-SAFE-WRAPPED RAISES (guaranteed, not scanned): 1
  bstpp\data_contracts.py:501

CLAUSE EVALUATION (population 3)
  ASCII      builder_max_sigma_invariant_clause
  ASCII      config_integral_invariant_clause
  ASCII      config_real_invariant_clause
  ASCII      min_sigma_positive_invariant_clause
  ASCII      panel_ratio_invariant_clause
  ASCII      polygon_min_sigma_invariant_clause
  ASCII      rectangle_bounds_invariant_clause
  ASCII      sigma_order_invariant_clause
  ASCII      support_mode_invariant_clause

INTERPOLATION HAZARD (informational, not a failure)
  NON_ASCII_PROPAGATING_SLOTS=0
  Slots rendering a caller-supplied value without numeric coercion,
  where a NON-ASCII argument reaches the message (OP-22).
  RESIDUAL, not covered by this count: _ascii_safe escapes non-ASCII
  but NOT ASCII control characters. Measured, because the general
  form of that claim is wrong: every !r slot is already safe, since
  repr() escapes a newline itself. The residual is confined to the
  PLAIN {name} slots of the CI-7/CI-8 clauses, and `name` is a
  call-site literal, never caller-supplied -- so it is reachable
  only by calling the clause function directly.
    (none)

FAIL 1 clause function(s) could not be evaluated -- an unevaluated clause is an UNCOVERED clause
  standardize_cov_invariant_clause: no sample for standardize_cov
      add a sample for it to CLAUSE_SAMPLES in _a26_ascii_sweep.py
SWEEP_EXIT:1

-- sample added; the sweep goes green on a LARGER population (10 clauses) --

PASS - 182/182 raise sites' text checked (100%); 10 clause functions evaluated, all ASCII
EXIT:0
