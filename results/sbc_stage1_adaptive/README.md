# Stage-1 SBC results (adaptive chain length)

Harness tip: `43556b4`. MACHINE-LOCAL — never compare ranks across machines.

**R = 600** — deliberate upward deviation from the runbook's pre-registered
R ≈ 100–200, decided **before** the run for power / resolution only
(validity unchanged).

`primary_pass: true` (R=600, L=127); zero divergences; 7/600 ESS retries
(max raw draws 1016). See `report.json` / `report.png`.

Quarantined fixed-length abort (do not pool): `results/sbc_stage1/` — evidence
that motivated Talts-style adaptive lengthening; incomplete at r=44 (ESS gate).
